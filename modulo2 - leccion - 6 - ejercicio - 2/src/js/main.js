'use strict';

'use strict';

const adalaber1 = {}
adalaber1.name = 'Susana';
adalaber1.age = '34';
adalaber1.profession = 'periodista';


adalaber1.speak = run => `Yo digo: ${run}`;
console.log(adalaber1.speak('estoy corriendo...'));
const runMarathon = (distance) => {
    return 50
};
adalaber1.speak = runMarathon => `un maratón de ${distance} km de distancia, con tó mi papo que los maratones son de 42 km.`;
const distance = 50;

console.log(adalaber1.speak('hola'));

